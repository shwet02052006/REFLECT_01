{
  "name": "chronos",
  "version": "1.0.0",
  "description": "A minimal, manual-entry focused reflection and productivity tracking application.",
  "scripts": {
    "start": "node server.js",
    "dev": "npm run install-all && concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend && npm install",
    "install-all": "npm run install-backend && npm run install-frontend",
    "build": "npm run install-all && cd frontend && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
